#include <iostream>
#include <windows.h>
#include <cmath>

using namespace std;

void Menu(double list[], int &amount);

int main(){
    SetConsoleOutputCP(65001);


    double list[10];
    int amount = 0;

    Menu(list, amount);


    
}